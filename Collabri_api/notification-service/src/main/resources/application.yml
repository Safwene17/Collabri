spring:
  application:
    name: notification
  config:
    import: optional:configserver:http://localhost:8888

springdoc:
  api-docs:
    path: /notification/v3/api-docs
  swagger-ui:
    enabled: false
